# SentimentAnalysis
A comparative benchmark evaluating the accuracy and computational trade-offs between classical linear boundaries (TF-IDF + Logistic Regression) and deep contextual multi-head attention networks (BERT) for large-scale social media sentiment classification.
